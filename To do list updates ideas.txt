Show a red warning if the user tries to add a task that already exists.

Save tasks to localStorage for persistence.

Display a confirmation message before deleting a task.

Add an edit button to rename tasks.

Sort tasks by status: completed vs. incomplete.

Add a button to delete all tasks at once.

Show the creation date next to each task.

Search bar to filter tasks by keywords.

Dark mode toggle for better user experience at night.

Allow users to prioritize tasks (e.g., High, Medium, Low).
